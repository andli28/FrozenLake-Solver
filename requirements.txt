# Generated with pipreqs, which only saves packages used in this project
# Run with pip install -r requirements.txt

gymnasium==0.27.1
numpy==1.24.2

# If you want to render with argument "human", you need to install pygame
pygame